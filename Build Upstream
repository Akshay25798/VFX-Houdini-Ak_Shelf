import os 
import hou
try:
    node = hou.pwd()
    path = node.evalParm("file")
    path = os.path.dirname(path)
    files = os.listdir(path)
    for file in files:
        if file.endswith(".ak"):
            return("Available")
except:
    return("Not available")
